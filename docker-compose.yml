version: '3.7'
services:
  postgres:
    image: postgres:12.0-alpine
    ports:
      - '3306:3306'
    volumes:
      - ./data:/var/lib/postgresql
    environment:
      - POSTGRES_USER=username
      - POSTGRES_PASSWORD=qwerty123
      - POSTGRES_DB=db